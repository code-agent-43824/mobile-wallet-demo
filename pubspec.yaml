name: mobile_wallet_demo
description: "Minimal Flutter mobile wallet demo scaffold."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number used as the build suffix.
version: 1.40.0+51

environment:
  sdk: ^3.11.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  bip32: ^2.0.0
  bip39: ^1.0.6
  convert: ^3.1.2
  cryptography: ^2.7.0
  flutter_secure_storage: ^10.3.1
  web3dart: ^3.0.1
  # web3dart 3.x moved EthereumAddress/EtherAmount into the `wallet` package and
  # no longer re-exports them, so we depend on it directly for those types.
  wallet: ^0.0.18
  local_auth: ^2.3.0
  file_selector: ^1.0.0
  image: ^4.0.0
  zxing2: ^0.2.0
  # Real WalletConnect v2 wallet SDK (Phase 9 chunk 9.2). Needs two toolchain
  # fixes (both applied): the `connectivity_plus` override below + the JitPack
  # Maven repo in android/build.gradle.kts (reown's `yttrium` native libs).
  # Android/iOS only; excluded cleanly on Windows.
  reown_walletkit: ^1.4.0
  # Camera QR scanning (Phase 9 chunk 9.9). Declares android/ios/macos/web only —
  # excluded on Windows (where file-load QR is the only path). Added dep-only first
  # (chunk 9.9a probe) to verify native iOS/Android builds before wiring the camera.
  mobile_scanner: ^7.2.0
  # EIP-4527 / Keystone BC-UR AirGap (Phase 12): MetaMask-compatible offline
  # signer. `cbor` = RFC8949 codec; `bc_ur` = UR / bytewords / multipart fountain
  # QR. Both pure Dart (no native), so no per-platform build risk.
  cbor: ^6.5.1
  bc_ur: ^1.0.0

# web3dart 3.x needs pointycastle ^4, but bip32/bip39 (latest 1.0.6, 5 yrs old)
# over-constrain it to ^3. They only use pointycastle's stable digest/HMAC/EC
# APIs (PBKDF2 seed, HMAC-SHA512 HD derivation, secp256k1), so forcing ^4 is
# safe — guarded by the deterministic derivation test (known mnemonic →
# 0xf39Fd6…266 in test/phone_secure_vault_test.dart).
dependency_overrides:
  pointycastle: ^4.0.0
  # reown_core 1.3.8 still caps this at 9.x, but 10.x retains the API it uses
  # and adds the supported Android cipher/migration implementation. Keep the
  # override covered by all platform builds until Reown widens its constraint.
  flutter_secure_storage: 10.3.1
  # reown pulls connectivity_plus; 7.1.x's iOS/macOS code uses
  # NWPath.isUltraConstrained (a too-new SDK symbol the CI Xcode lacks). Pin to
  # the clean 7.0.0 — within reown_core's >=6.1.3 <8.0.0, same 7.x Dart API.
  connectivity_plus: 7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in the "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
